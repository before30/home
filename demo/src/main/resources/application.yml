server:
  port: 8090

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/

spring:
  boot:
    admin:
      client:
        url: http://localhost:8081

---

spring:
  profiles: development
server:
  port: 8080

---

spring:
  profiles: production
server:
  port: 8080
